{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run the app",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "dlvFlags": ["--check-go-version=false"],
      "program": "${fileDirname}",
      "envFile": "${workspaceFolder}/.env.debug"
    }
  ]
}